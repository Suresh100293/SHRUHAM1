@model ALT_ERP3.Areas.Logistics.Models.UserDashboardVM
@using Common;
<script>
     $(document).ready(function () {
        $("#dynamic-table").on('click', 'tbody tr', function (event) {
            event.preventDefault();
            var PSerial = [];
            var row = $(this);
            var $tds = $(this).find('td');
            psrl = $tds.eq(0).text();
            PSerial.push(psrl);
            var ModelData = {};
            ModelData["User"] = PSerial.toString();
            $("#User").val(PSerial.toString());

            var DTO = { mModel: ModelData };
            var url = '@Url.Action("ClickLeftGrid")';
            $.ajax({
                type: "POST",
                dataType: "json",
                data: JSON.stringify(DTO),
                cache: false,
                url: url,
                contentType: "application/json;charset=utf-8",
                beforesend: function () {
                    $('.spinner').show();
                },
                success: function (data) {
                    $('.spinner').hide();
                    $('#errormessage').hide();
                    $('#MessageRulesdiv').html(data.Html);
                    $('#SelectedUserName').val(data.SelectedUserName);
                },
                error: function () {
                    $('.spinner').hide();
                    alert("error occured while processing your request.");
                }
            });
        });
    })
</script>
<table class="zui-table" id="dynamic-table" style="min-height:360px;max-height:360px;overflow:scroll;">
    <thead>
        <tr role="row">
            <th style="width: 14%;vertical-align:inherit;">Code</th>
            <th style="width: 42%;vertical-align:inherit;">Name</th>
        </tr>
    </thead>
    <tbody>
        @if (Model.mLeftList != null)
        {
            foreach (var item in @Model.mLeftList)
            {
                <tr style="font-family:Tahoma;font-size:9pt;border: solid 1px lightgray;">
                    <td style="width: 14%;vertical-align:inherit;">@item.User</td>
                    <td style="width: 42%;vertical-align:inherit;">@item.UserL</td>
                </tr>
            }
        }
    </tbody>
</table>

